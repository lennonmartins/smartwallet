{
  "name": "smartwallet",
  "version": "1.0.0",
  "description": "Gerenciador Financeiro Inteligente",
  "main": "src/index.ts",
  "scripts": {
    "start": "npm run build && node build/index.js",
    "build": "rimraf ./build && tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "npx nodemon"
  },
  "author": "Lennon Martins da Silva",
  "license": "ISC",
  "dependencies": {
    "smartwallet": "file:"
  },
  "devDependencies": {
    "@types/node": "^22.1.0",
    "nodemon": "^3.1.4",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
